# قواعد العمل في منصة إحياء

هذا الملف ملزم لأي مطور أو أداة AI تعمل على المشروع.

## قواعد لا تُكسر

1. لا تغيّر التصميم العام، بنية المشروع، نموذج البيانات، أو نظام الصلاحيات إلا بطلب صريح من مالك المشروع.
2. الواجهة عربية أولًا: حافظ على `dir="rtl"`، المصطلحات العربية الواضحة، وتجربة الجوال الحالية.
3. لا تستخدم **Mock data** في واجهات الإدارة أو المعلم التي ينبغي أن تعرض بيانات فعلية؛ مصدر الحقيقة هو API وقاعدة PostgreSQL.
4. لا ترفع أو تكتب قيم `.env` أو مفاتيح API أو `DATABASE_URL` أو كلمات مرور أو نسخ قواعد البيانات في Git.
5. لا تعدّل الخادم، الدومين، قاعدة بيانات الإنتاج، أو النشر الحي إلا بإذن صريح من المالك وبعد نسخة احتياطية مناسبة.
6. افصل دائمًا بين الصلاحيات: الإدارة ترى وتدير كل البيانات؛ المعلم يرى حلقاته وطلابه وسجلاته المسموح بها فقط.
7. التزم بالهوية الحالية: بترولي هادئ، ذهبي مقتصد، أسطح عاجية، وخطوط Noto Sans Arabic وAmiri. لا تحوّل الواجهة إلى تصميم SaaS أجنبي أو شديد الألوان دون طلب صريح.

## قبل أي تغيير كبير

- افحص الملفات والمسارات المتأثرة أولًا، واشرح الأثر المتوقع ونطاقه.
- لا تنقل الطلاب أو تغيّر السجلات التاريخية بطريقة تمحو الأثر: انتقال الطالب يجب أن يعرض «من → إلى» ويُسجل في `student_transfer_history`.
- حافظ على التوافق مع الجداول والمهاجرات القائمة؛ أي تغيير في البيانات يجب أن يأتي عبر migration جديد قابل للمراجعة.

## بعد كل تغيير

- شغّل `npm run build`، والاختبارات المناسبة للتغيير. الحد الأدنى لمسارات الاستضافة هو `npm run test:sites`.
- اختبر الصلاحية ذات العلاقة يدويًا أو باختبار آلي: لا يكفي نجاح البناء لتأكيد أن بيانات المعلم أو الإدارة صحيحة.
- اذكر بوضوح الملفات المعدلة، ما اختبرته، وأي قيد أو مخاطرة باقية.

## قرارات المنتج الثابتة حاليًا

- الحلقات تعقد افتراضيًا السبت والأحد.
- السبت للحضور فقط، والأحد يدعم الحضور ومتابعة القرآن للطالب الحاضر؛ لا تدخل متابعة قرآن للغائب أو المعتذر أو المسافر.
- الحضور يُحفظ آليًا ولا يحتاج اعتمادًا إداريًا كي يظهر، لكن الإدارة تملك اعتماد الجلسات عند الحاجة.
- يجب أن تحفظ تحديثات الحضور والتسميع بنمط upsert حتى لا تتكرر سجلات الطالب في اليوم والجسلة نفسيهما.
- أرقام لوحة الإدارة وتقاريرها تبنى من قاعدة البيانات الفعلية لا من أرقام ثابتة.
- انتقال الطالب يحتفظ بالحضور والتسميع السابقين، مع سجل إداري يوضح الحلقة السابقة والحلقة الجديدة ومن نفذ النقل وتاريخه.

## خريطة ملكية الملفات

- `src/`: واجهة React وتصميمها.
- `server/index.mjs`: API والمصادقة ونقاط الدخول.
- `server/admin-data.mjs`: عمليات الإدارة والتقارير.
- `server/teacher-data.mjs`: عمليات المعلم وتسجيل الجلسات.
- `server/schema.sql` و`server/migrations/`: بنية PostgreSQL وترقياتها.
- `tests/`: اختبارات الجدولة والاستضافة واختبار API التشغيلي.

راجع `docs/PROJECT_HANDOVER.md` قبل إجراء أي توسعة وظيفية.
